import { SceneEventsDirective } from './scene-events.directive';

describe('SceneEventsDirective', () => {
  it('should create an instance', () => {
    const directive = new SceneEventsDirective();
    expect(directive).toBeTruthy();
  });
});
